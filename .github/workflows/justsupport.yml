import React from 'react';
import { Button } from '@/components/ui/button';
import { Card, CardContent } from '@/components/ui/card';
import { Sparkles, HandHelping, HeartHandshake } from 'lucide-react';

export default function JustSupportHome() {
  return (
    <div className="min-h-screen bg-gray-50 text-gray-800">
      {/* Header */}
      <header className="bg-indigo-600 text-white py-6 px-4 shadow-md">
        <h1 className="text-3xl font-bold">JustSupport</h1>
        <p className="text-lg">Caring for the Aged and Disabled in the UK</p>
      </header>

      {/* Hero Section */}
      <section className="bg-white py-16 px-4 text-center">
        <h2 className="text-4xl font-semibold mb-4">Support with Compassion</h2>
        <p className="text-lg max-w-2xl mx-auto mb-6">
          JustSupport is a UK-based charity that offers community-driven, affordable care and companionship
          to the elderly and disabled who cannot access traditional care services.
        </p>
        <Button className="text-white bg-indigo-600 hover:bg-indigo-700">Donate Now</Button>
      </section>

      {/* Services */}
      <section className="py-12 px-4">
        <h3 className="text-2xl font-bold text-center mb-8">Our Services</h3>
        <div className="grid md:grid-cols-3 gap-6">
          <Card>
            <CardContent className="flex flex-col items-center py-6">
              <HeartHandshake className="h-10 w-10 text-indigo-600 mb-4" />
              <h4 className="text-xl font-semibold mb-2">Home Visits</h4>
              <p className="text-center text-sm">Friendly companionship and practical help directly in homes.</p>
            </CardContent>
          </Card>
          <Card>
            <CardContent className="flex flex-col items-center py-6">
              <HandHelping className="h-10 w-10 text-indigo-600 mb-4" />
              <h4 className="text-xl font-semibold mb-2">Daily Support</h4>
              <p className="text-center text-sm">Meal prep, light chores, and mobility support to make life easier.</p>
            </CardContent>
          </Card>
          <Card>
            <CardContent className="flex flex-col items-center py-6">
              <Sparkles className="h-10 w-10 text-indigo-600 mb-4" />
              <h4 className="text-xl font-semibold mb-2">Digital Literacy</h4>
              <p className="text-center text-sm">Helping our beneficiaries stay connected online and use care tools.</p>
            </CardContent>
          </Card>
        </div>
      </section>

      {/* Contact & Chatbot Embed */}
      <section className="bg-indigo-100 py-12 px-4 text-center">
        <h3 className="text-2xl font-bold mb-4">Get in Touch</h3>
        <p className="mb-6">Have questions? Chat with us or reach out for support.</p>
        <Button className="bg-indigo-600 text-white hover:bg-indigo-700">Contact Us</Button>

        {/* Chatbot Embed Placeholder (Replace with Tidio or Chatbot.com script in deployment) */}
        <div className="mt-10">
          <p className="text-sm text-gray-600">Live Chat Powered by Tidio</p>
          <div className="border rounded p-4 bg-white mt-2 text-left text-sm text-gray-700">
            Chatbot Placeholder - Embed your preferred chatbot JavaScript snippet here for deployment.
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="bg-indigo-600 text-white text-center py-4 mt-10">
        <p className="text-sm">&copy; {new Date().getFullYear()} JustSupport Charity. Registered in the UK. All rights reserved.</p>
      </footer>
    </div>
  );
}
